package com.example.vidmot;

public class Animation {

  /*  private Audio audio = new Audio();
    private BouncingController bc = audio.getBC();
    private BoltiC bolti = bc.getFxLeikbord().getFxBolti();
    private Timeline animeTime;


    public Animation() throws IOException {
    }
*/
   /* public void playAnimation(double millis, double startA, double startB){
        KeyValue kv = new KeyValue(startA, startB);
        KeyFrame k = new KeyFrame(Duration.millis(millis), kv);
        animeTime = new Timeline(k);
        animeTime.setCycleCount(Timeline.INDEFINITE);
        animeTime.play();
    }

    public void paint(double x, double y, double d) {
        x += bolti.getX();
        y += bolti.getY();
        d += bolti.getFitWidth()/2;
        Ellipse ellipse = new Ellipse(x, y, d, d);
        playAnimation(100, 10, 10);
        bc.fxRoot.getChildren().add(ellipse);
        if (x<20) {
            paint(x, y, d/2);
        }
    }

    public void endTree() {
    } */
}